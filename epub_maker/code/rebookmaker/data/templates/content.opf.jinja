<package xmlns="http://www.idpf.org/2007/opf" version="3.0" unique-identifier="bookId">

<!-- Generated file, modifying it is futile. -->

<metadata xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:opf="http://www.idpf.org/2007/opf">
    <dc:title>{{ title }}</dc:title>
    <dc:creator id="creator1">{{ author_name }}</dc:creator>
    <meta property="dcterms:modified">{{ modified_date }}</meta>
    <meta refines="#creator1" property="role" scheme="marc:relators" id="role">aut</meta>
    <meta refines="#creator1" property="file-as">{{ author_sorted }}</meta>
    <dc:rights>{{ dc_rights }}</dc:rights>
    <dc:identifier id="bookId">{{ url }}</dc:identifier>
    <dc:publisher>{{ publisher }}</dc:publisher>
    <dc:language>{{ language }}</dc:language>
    <meta name="cover" content="coverimage" />
</metadata>

<manifest>
{% for img in images0 %}    <item id="{{ img.id }}" href="{{ img.href }}" media-type="{{ img.media_type }}" {% if img.media_type == 'image/webp' %}fallback="onepixel_png"{% endif %} />
{% endfor %}
{% for img in images1 %}    <item id="{{ img.id }}" href="{{ img.href }}" media-type="{{ img.media_type }}" {% if img.media_type == 'image/webp' %}fallback="onepixel_png"{% endif %} />
{% endfor %}
{% if found_webp %}
<item href="onepixel.png" id="onepixel_png" media-type="image/png" />
{% endif %}
<item id="nav" media-type="application/xhtml+xml" properties="nav" href="nav.xhtml"/>
<item id="ncx" media-type="application/x-dtbncx+xml" href="toc.ncx"/>

{% for item in htmls0 %}    <item id="{{ item.id }}" media-type="application/xhtml+xml" href="{{ item.href }}"/>
{% endfor %}
    <item id="css" href="style.css" media-type="text/css"/>
</manifest>

<spine toc="ncx">
{% for item in htmls0 %}    <itemref idref="{{ item.id }}"/>
{% endfor %}</spine>

{% if guide %}<guide>
{% for g in guide %}    <reference type="{{ g.type }}" title="{{ g.title }}" href="{{ g.href }}"/>
{% endfor %}</guide>
{% endif %}
</package>
